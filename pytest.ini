# Quick smoke test for the LLM agent (no audio, no Twilio).
# Requires: OPENAI_API_KEY in env, pip install -e .

[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q
markers =
    unit: fast, hermetic unit tests
